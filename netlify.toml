# Static build. No adapter and no server runtime: the contact page reaches
# WhatsApp and email directly, so there is nothing to run.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
